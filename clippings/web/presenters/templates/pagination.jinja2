<div class="pagination">
    {% for page in pages %}
    {% if page.url %}
    <a href="{{ page.url }}">{{ page.text }}</a>
    {% else %}
    <span>{{ page.text }}</span>
    {% endif %}
    {% endfor %}
</div>
